#This is the main idea of this yaml file. YEET!

steps:
#this step builds the image
- name: 'gcr.io/cloud-builders/docker'
  id: Build Docker Image
  args:
  - 'build'
  - '.'
  - '-t'
  - 'gcr.io/$PROJECT_ID/team-1-website:latest'
  
#this step pushes the image
- name: 'gcr.io/cloud-builders/docker'
  id: Push 
  args: 
  - 'push'
  - 'gcr.io/$PROJECT_ID/team-1-website:latest'

  # This step deploys the application in Kubernetes
- name: 'gcr.io/cloud-builders/gke-deploy'
  id: Deploy Pod to Kubernetes
  args:
  - run
  - --filename=jade-pod.yaml
  - --image=gcr.io/$PROJECT_ID/team-1-website:$SHORT_SHA
  - --location=us-central1-c
  - --cluster=cluster-1